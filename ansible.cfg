[defaults]
host_key_checking = False
executable = /bin/bash
inventory = azure_rm.yaml
remote_user = azureuser
private_key_file = ~/.ssh/my-app-key-pair.pem
VAULT_PASSWORD_FILES = ~/vault_file
enable_plugins = host_list, script, auto, yaml, ini, toml, azure_rm
